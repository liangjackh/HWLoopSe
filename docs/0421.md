Root Cause 1: Constant-Only Assertions (p6, p8)                                                                      │
│                                                                                                                      │
│ Properties that reference only preprocessor defines/constants (e.g., GPIO_START_ADDR == 32'h1A101000) produce empty  │
│ seed signals for COI analysis. The COI analyzer's extract_signals_from_condition() cannot extract signal names from  │
│ numeric literals. Result:                                                                                            │
│ - seed_signals = [] (empty)                                                                                          │
│ - COI analysis is skipped entirely (line 933-935 in execution_engine.py: if not coi_result.relevant_instances)       │
│ - All 600+ modules remain in cfgs_by_module                                                                          │
│ - Path explosion: every module's CFGs are executed at every cycle                                                    │
│ - Even with only 2 milestones, the search space is massive                                                           │
│                                                                                                                      │
│ Evidence from logs:                                                                                                  │
│ - p6 log: 0 [COI] lines printed (COI never ran)                                                                      │
│ - p6 log: timeout after printing 800+ wire groups (all modules loaded)                                               │
│ - p6 milestone file: target_expr contains only constants: 32'h1A101000 != 32'h1A101000 || 32'h1A10AFFF !=            │
│ 32'h1A101FFF                                                                                      

Root Cause 2: Path Explosion in CFG Construction (p11, p13, p14, p27)                                                │
│                                                                                                                      │
│ Properties that reference signals deep in the RISC-V core hierarchy (e.g.,                                           │
│ top_wrapper.riscv_core.debug_unit_i.dbg_halt) pull the entire riscv_core module into the COI. The RISC-V core is a   │
│ complex processor with:                                                                                              │
│ - 80 relevant CFGs across 21 submodules (from p11 log)                                                               │
│ - Some CFGs have hundreds or thousands of paths due to deeply nested case statements                                 │
│ - The compressed_decoder_i module has nested case statements that create 256-512+ paths                              │
│ - Path enumeration via nx.all_simple_paths() (line 732 in cfg.py) can hang for CFGs with exponential path counts     │
│                                                                                                                      │
│ The hang location: The engine hangs during CFG list building (lines 936-949 in strategies.py) when accessing         │
│ cfg.paths for a CFG with many paths. The [CFG Summary] line never prints, confirming the hang occurs before the CFG  │
│ execution loop starts.                                                                                               │
│                                                                                                                      │
│ Evidence from logs:                                                                                                  │
│ - p11 log: COI correctly identifies 80 relevant CFGs                                                                 │
│ - p11 log: Only 1 [Skip] line printed, then timeout                                                                  │
│ - p11 log: No [CFG Summary] line (hangs during CFG list building, before execution loop)                             │
│ - p11 log: The [Skip] is from a previous cycle or work item (not cycle 0)                                            │
│ - p2_fixed (working case): COI prunes to just soc_interconnect + XBAR_L2_i (simpler modules, fewer CFGs, fewer paths │
│  per CFG)                                                  
[200~Root Cause 3: Infinite Cycle Loop (All Properties)                                                                   │
│                                                                                                                      │
│ When all CFGs in a cycle are skipped/abandoned, the engine does NOT detect this condition and unconditionally        │
│ enqueues a new work item for the next cycle with identical state. This creates an infinite-like loop through all 30  │
│ cycles.                                                                                                              │
│                                                                                                                      │
│ The bug location: engine/strategies.py, lines 959-1166 in _execute_cycle():                                          │
│ 1. Lines 959-1072: CFG loop iterates through remaining_cfgs                                                          │
│ 2. Lines 1063-1072: When a CFG is abandoned, continue to next CFG                                                    │
│ 3. Missing logic: No tracking of whether ANY CFG actually executed                                                   │
│ 4. Lines 1154-1166: Unconditionally enqueue next cycle if SAT (assumes at least one CFG executed)                    │
│ 5. Result: If all CFGs are skipped, state is unchanged, path condition remains SAT, next cycle is enqueued with same │
│  state → infinite loop                                                                                               │
│                                                                                                                      │
│ Evidence from logs:                                                                                                  │
│ - p2_fixed log: Shows many [Skip] lines but also [Enqueue] lines (some CFGs succeed, progress is made)               │
│ - p11 log: Only 1 [Skip] line, then timeout (stuck in CFG execution, not cycle loop)                                 │
│                                                     
]
Implementation Plan                                                                                                  │
│                                                                                                                      │
│ Fix 1: Handle Constant-Only Assertions (p6, p8)                                                                      │
│                                                                                                                      │
│ File: engine/execution_engine.py                                                                                     │
│                                                                                                                      │
│ Location: Lines 867-910 (seed signal extraction from milestones/assertions)                                          │
│                                                                                                                      │
│ Change: When extract_signals_from_condition() returns only numeric literals (no actual signal names), fall back to a │
│  minimal COI seed instead of skipping COI entirely.                                                                  │
│                                                                                                                      │
│ Approach:                                                                                                            │
│ 1. After extracting signals from target_expr and milestone conditions (lines 867-894), check if seed_signals is      │
│ empty                                                                                                                │
│ 2. If empty, add a fallback seed: the assertion-containing instance itself (e.g., jg_bind_inst) and its direct       │
│ parent (e.g., top_wrapper)                                                                                           │
│ 3. This ensures COI runs and prunes at least some modules, even if the assertion is constant-based


