Now I have a complete picture. Let me form the todo.
                                         
  ---                                                                                                          
  The three causes from the previous analysis are partially correct but the real breakdown is:                 
                                                                                                               
  - Cause 1 (assign-driven, COI finds no CFGs): True and the root cause. But the underlying reason is more     
  specific: COI step 3 correctly traces soc_interconnect.TCDM_data_gnt_DEM_TO_XBAR → through the port map → to
  XBAR_L2_i.data_gnt_o. But then step 3's reverse direction (port_map_parent_to_child) doesn't help because
  data_gnt_o is an output of XBAR_L2_i — the COI needs to trace into XBAR_L2_i to find what writes data_gnt_o
  inside it. The analyze loop only adds the child port to the worklist but XBAR_L2_i has no entry in
  cfgs_by_module (because it's a separate module definition, not compiled as a named instance with CFGs). So
  the chain breaks there.
  - Cause 2 (no reset modeled): Secondary/irrelevant for this specific case — even with reset, these are
  combinational signals. The real fix is not about reset.
  - Cause 3 (spurious witness): Symptom, not cause — flows from Cause 1.

  Here is the todo:

● ---                                    
  Here is the precise diagnosis and plan:                          
                                                                                                               
  The previous analysis reasons 1–3 are:                                                                       
  - Reason 1 ✓ True and is the actual root cause — but the precise failure point is: COI traces                
  soc_interconnect.TCDM_data_gnt_DEM_TO_XBAR → XBAR_L2_i.data_gnt_o correctly via port_map_parent_to_child, but
   then stops because XBAR_L2_i has no CFG entries in cfgs_by_module. The COI never enters XBAR_L2_i's         
  internals to find what drives data_gnt_o.                                        
  - Reason 2 ✗ Not the real cause here — TCDM_data_gnt and TCDM_data_add are combinational, so reset state is
  irrelevant. Even after reset these signals depend only on current request inputs.
  - Reason 3 ✓ Correct — but it's the symptom, not the cause. The spurious witness exists because Reason 1 left
   the signals unconstrained.

  Todo (2 tasks, Task #6 blocked on Task #5):

  ┌─────┬────────────────────────────────────────────────────────────────────────┬──────────────────────────┐
  │  #  │                                  Task                                  │          Where           │
  ├─────┼────────────────────────────────────────────────────────────────────────┼──────────────────────────┤
  │     │ Fix COIAnalyzer.analyze() to trace through submodule output ports into │                          │
  │ 5   │  their internals — when (child_inst, port_name) is enqueued and        │ frontend/coi_analyzer.py │
  │     │ child_inst exists in cfgs_by_module, the backward walk should continue │                          │
  │     │  inside the child                                                      │                          │
  ├─────┼────────────────────────────────────────────────────────────────────────┼──────────────────────────┤
  │ 6   │ Re-run hackdac18, verify Relevant CFGs now contains XBAR_L2_i entries  │ verification             │
  │     │ and the cycle-0 spurious violation is gone                             │                          │
  └─────┴────────────────────────────────────────────────────────────────────────┴──────────────────────────┘

  Shall I implement Task #5 now?

✻ Sautéed for 2m 59s                     
                                                                   
  2 tasks (0 done, 2 open)                                                
  ◻ Fix COI: trace through output ports into submodule internals
  ◻ Verify: no spurious cycle-0 violation after COI fix › blocked by #5
