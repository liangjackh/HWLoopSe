{
  "target": "Violate HACKDAC_p84: when dmi_req_ready==1 and state_q==Write, next cycle state_q must be WaitWriteValid. Find a state where that transition does not occur.",
  "target_expr": "top_wrapper_dac21.dmi_jtag_i.dmi_req_ready == 1 && top_wrapper_dac21.dmi_jtag_i.state_q == 2 && top_wrapper_dac21.dmi_jtag_i.state_q != 3",
  "milestones": [
    {
      "step": 0,
      "description": "System in reset",
      "condition": "rst_ni == 0",
      "expected_cycles": 1
    },
    {
      "step": 1,
      "description": "Reset deasserted, JTAG state machine in Idle (state_q==0)",
      "condition": "rst_ni == 1 && top_wrapper_dac21.dmi_jtag_i.state_q == 0",
      "expected_cycles": 2
    },
    {
      "step": 2,
      "description": "JTAG transitions to Write state (state_q==2)",
      "condition": "top_wrapper_dac21.dmi_jtag_i.state_q == 2",
      "expected_cycles": 4
    },
    {
      "step": 3,
      "description": "Violation: dmi_req_ready asserted in Write state but state_q does not transition to WaitWriteValid (3) — unreachable state bug",
      "condition": "top_wrapper_dac21.dmi_jtag_i.dmi_req_ready == 1 && top_wrapper_dac21.dmi_jtag_i.state_q == 2",
      "expected_cycles": 3
    }
  ]
}
