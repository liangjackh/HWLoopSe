{
  "target": "Violate HACKDAC_p29: on reset (!rstn_top), aes_out and c in mux_func must be 0. Find a state where reset is asserted but outputs are non-zero (MAC output not erased on reset).",
  "target_expr": "rstn_top == 0 && (top_wrapper.mux_func.aes_out != 128'h00000000000000000000000000000000 || top_wrapper.mux_func.c != 128'h00000000000000000000000000000000)",
  "milestones": [
    {
      "step": 0,
      "description": "System reset applied",
      "condition": "rstn_top == 0",
      "expected_cycles": 1
    },
    {
      "step": 1,
      "description": "Reset released, mux_func computes non-zero outputs",
      "condition": "rstn_top == 1 && top_wrapper.mux_func.c != 128'h00000000000000000000000000000000",
      "expected_cycles": 2
    },
    {
      "step": 2,
      "description": "Violation: reset reasserted but aes_out or c remain non-zero (outputs not cleared on reset)",
      "condition": "rstn_top == 0 && (top_wrapper.mux_func.aes_out != 128'h00000000000000000000000000000000 || top_wrapper.mux_func.c != 128'h00000000000000000000000000000000)",
      "expected_cycles": 1
    }
  ]
}
