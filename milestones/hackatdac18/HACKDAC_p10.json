{
  "target": "Violate HACKDAC_p10: when passchk is asserted, bitindex must equal 32. Find a state where passchk==1 but bitindex!=32.",
  "target_expr": "top_wrapper.adbg_tap_top.passchk == 1 && top_wrapper.adbg_tap_top.bitindex != 32",
  "milestones": [
    {
      "step": 0,
      "description": "System reset applied, JTAG TAP reset asserted",
      "condition": "rstn_top == 0",
      "expected_cycles": 1
    },
    {
      "step": 1,
      "description": "Reset released, JTAG TAP reset deasserted, counters initialized to zero",
      "condition": "rstn_top == 1 && top_wrapper.adbg_tap_top.trstn_pad_i == 1",
      "expected_cycles": 1
    },
    {
      "step": 2,
      "description": "Password check in progress, correct counter accumulating",
      "condition": "top_wrapper.adbg_tap_top.correct > 0 && top_wrapper.adbg_tap_top.bitindex > 0",
      "expected_cycles": 5
    },
    {
      "step": 3,
      "description": "Violation: passchk asserted but bitindex has not reached 32 (advanced debug unit only checks 31 bits)",
      "condition": "top_wrapper.adbg_tap_top.passchk == 1 && top_wrapper.adbg_tap_top.bitindex != 32",
      "expected_cycles": 5
    }
  ]
}
