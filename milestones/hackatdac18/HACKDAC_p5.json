{
  "target": "Cover HACKDAC_p5: find a state where HRESETn is deasserted while r_gpio_lock is non-zero (reset does not clear the lock register).",
  "target_expr": "top_wrapper.apb_gpio.HRESETn == 0 && top_wrapper.apb_gpio.r_gpio_lock != 32'h00000000",
  "milestones": [
    {
      "step": 0,
      "description": "System reset applied",
      "condition": "rstn_top == 0",
      "expected_cycles": 1
    },
    {
      "step": 1,
      "description": "Reset released, GPIO lock register initialized to zero",
      "condition": "rstn_top == 1 && top_wrapper.apb_gpio.HRESETn == 1",
      "expected_cycles": 1
    },
    {
      "step": 2,
      "description": "APB write sets GPIO lock register to non-zero value",
      "condition": "top_wrapper.apb_gpio.r_gpio_lock != 32'h00000000",
      "expected_cycles": 2
    },
    {
      "step": 3,
      "description": "Cover: HRESETn deasserted while lock register is non-zero (reset does not clear lock)",
      "condition": "top_wrapper.apb_gpio.HRESETn == 0 && top_wrapper.apb_gpio.r_gpio_lock != 32'h00000000",
      "expected_cycles": 1
    }
  ]
}
