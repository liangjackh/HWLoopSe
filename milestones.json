{
  "target": "Violate assertion 'out <= 2' in place_holder",
  "target_expr": "place_holder.out > 2",
  "milestones": [
    {
      "step": 0,
      "description": "System Reset",
      "condition": "RST == 1"
    },
    {
      "step": 1,
      "description": "Reset released, first clock edge increments out to 1",
      "condition": "RST == 0 && out == 1"
    },
    {
      "step": 2,
      "description": "Second clock edge increments out to 2",
      "condition": "RST == 0 && out == 2"
    },
    {
      "step": 3,
      "description": "Third clock edge increments out to 3, satisfying target",
      "condition": "out > 2"
    },
    {
      "step": 4,
      "description": "System Reset",
      "condition": "RST == 1"
    },
    {
      "step": 5,
      "description": "Release Reset and allow first increment",
      "condition": "RST == 0"
    },
    {
      "step": 6,
      "description": "First increment after reset, out becomes 1",
      "condition": "out == 1"
    },
    {
      "step": 7,
      "description": "Second increment, out becomes 2",
      "condition": "out == 2"
    },
    {
      "step": 8,
      "description": "Third increment, target reached (out > 2)",
      "condition": "out > 2"
    }
  ]
}