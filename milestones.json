{
  "target": "Violate assertion 'out <= 3' in test_1",
  "target_expr": "test_1.out > 3",
  "milestones": [
    {
      "step": 0,
      "description": "System Reset",
      "condition": "RST == 1"
    },
    {
      "step": 1,
      "description": "Reset released, out starts incrementing from 0",
      "condition": "RST == 0 && test_1.out == 0"
    },
    {
      "step": 2,
      "description": "First increment after reset",
      "condition": "RST == 0 && test_1.out == 1"
    },
    {
      "step": 3,
      "description": "Second increment after reset",
      "condition": "RST == 0 && test_1.out == 2"
    },
    {
      "step": 4,
      "description": "Third increment after reset, reaching the assertion boundary",
      "condition": "RST == 0 && test_1.out == 3"
    },
    {
      "step": 5,
      "description": "Fourth increment after reset, violating the assertion and reaching the target",
      "condition": "test_1.out > 3"
    }
  ]
}