{
  "target": "Violate assertion 'out <= 2' in test_1",
  "target_expr": "test_1.out > 2",
  "milestones": [
    {
      "step": 0,
      "description": "System Reset",
      "condition": "RST == 1"
    },
    {
      "step": 1,
      "description": "Reset released, counter starts incrementing",
      "condition": "RST == 0 && test_1.out == 0"
    },
    {
      "step": 2,
      "description": "Counter increments to 1",
      "condition": "RST == 0 && test_1.out == 1"
    },
    {
      "step": 3,
      "description": "Counter increments to 2",
      "condition": "RST == 0 && test_1.out == 2"
    },
    {
      "step": 4,
      "description": "Counter increments to 3, violating the assertion",
      "condition": "test_1.out > 2"
    }
  ]
}