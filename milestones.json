{
  "target": "Violate assertion 'out <= 2' in place_holder",
  "target_expr": "place_holder.out > 2",
  "milestones": [
    {
      "step": 0,
      "description": "System Reset",
      "condition": "RST == 1"
    },
    {
      "step": 1,
      "description": "Release Reset and allow first increment",
      "condition": "RST == 0 && out == 0"
    },
    {
      "step": 2,
      "description": "First increment after reset",
      "condition": "out == 1"
    },
    {
      "step": 3,
      "description": "Second increment after reset",
      "condition": "out == 2"
    },
    {
      "step": 4,
      "description": "Third increment after reset, target reached",
      "condition": "out > 2"
    },
    {
      "step": 5,
      "description": "System Reset",
      "condition": "RST == 1"
    },
    {
      "step": 6,
      "description": "Reset deasserted, first clock edge increments out to 1",
      "condition": "RST == 0 && out == 1"
    },
    {
      "step": 7,
      "description": "Second clock edge increments out to 2",
      "condition": "RST == 0 && out == 2"
    },
    {
      "step": 8,
      "description": "Third clock edge increments out to 3, satisfying target",
      "condition": "out > 2"
    }
  ]
}